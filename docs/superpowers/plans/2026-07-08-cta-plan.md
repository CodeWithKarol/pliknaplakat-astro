# CTA Section Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement the dual-action CTA section after the Storytelling content.

**Architecture:** Use a distinct background color section with flex-based button alignment.

**Tech Stack:** Astro, Tailwind CSS (v4), shadcn/ui.

---

## Chunk 1: Implementation

### Task 1: Update `src/pages/index.astro`

**Files:**
- Create: `src/lib/cta-content.ts`
- Modify: `src/pages/index.astro`

- [ ] **Step 1: Create `src/lib/cta-content.ts`**

```typescript
export const ctaContent = {
  heading: "Gotowy na zmianę przestrzeni?",
  primary: "Zobacz najnowszą kolekcję",
  secondary: "Pobierz darmową próbkę"
}
```

- [ ] **Step 2: Add CTA section to `src/pages/index.astro`**

```astro
---
import { ctaContent } from "@/lib/cta-content"
// ... existing imports
---

<!-- ... Storytelling Section ... -->

<!-- CTA Section -->
<section class="bg-muted/50 py-20 px-6">
  <div class="max-w-3xl mx-auto text-center">
    <h2 class="text-3xl md:text-4xl font-bold tracking-tight text-foreground">
      {ctaContent.heading}
    </h2>
    <div class="flex flex-col sm:flex-row justify-center items-center gap-6 mt-10">
      <Button size="lg">{ctaContent.primary}</Button>
      <Button variant="outline" size="lg">{ctaContent.secondary}</Button>
    </div>
  </div>
</section>
```

- [ ] **Step 3: Commit**

```bash
git add src/pages/index.astro src/lib/cta-content.ts
git commit -m "feat: implement dual-action cta section"
```

## Chunk 2: Verification

### Task 2: Verify Standards

- [ ] **Step 1: Run build and lint**

Run: `npm run lint && npm run typecheck && npm run build`
Expected: PASS

- [ ] **Step 2: Final commit**

```bash
git commit -am "chore: verify cta implementation"
```
