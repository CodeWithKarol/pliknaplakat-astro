# FAQ Section Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement the FAQ section in `src/pages/index.astro`.

**Architecture:** Use a clean, text-based list layout for maximum SEO impact and readability.

**Tech Stack:** Astro, Tailwind CSS (v4).

---

## Chunk 1: Implementation

### Task 1: Update `src/pages/index.astro`

**Files:**
- Create: `src/lib/faq-content.ts`
- Modify: `src/pages/index.astro`

- [ ] **Step 1: Create `src/lib/faq-content.ts`**

```typescript
export const faqContent = [
  {
    question: "Co otrzymuję po zakupie?",
    answer: "Otrzymujesz pliki cyfrowe w wysokiej rozdzielczości, które możesz samodzielnie wydrukować w dowolnym formacie."
  },
  {
    question: "Czy te plakaty do druku wymagają specjalnego papieru?",
    answer: "Tak, wyjaśniam to w darmowym poradniku, który dostaniesz po zapisie."
  },
  {
    question: "Jaka jest różnica między grafiką w stylu retro a nowoczesną fotografią?",
    answer: "Różnią się techniką wykonania i klimatem, co opisuję przy poszczególnych kolekcjach."
  }
]
```

- [ ] **Step 2: Add FAQ section to `src/pages/index.astro`**

```astro
---
import { faqContent } from "@/lib/faq-content"
// ... existing imports
---

<!-- ... Education Section ... -->

<!-- FAQ Section -->
<section class="max-w-3xl mx-auto px-6 py-24">
  <h2 class="text-3xl font-bold text-center text-foreground">
    Najczęściej zadawane pytania
  </h2>
  
  <div class="space-y-10 mt-16">
    {faqContent.map((faq) => (
      <div>
        <h3 class="text-lg font-semibold text-foreground">
          {faq.question}
        </h3>
        <p class="text-muted-foreground mt-2">
          {faq.answer}
        </p>
      </div>
    ))}
  </div>
</section>
```

- [ ] **Step 3: Commit**

```bash
git add src/pages/index.astro src/lib/faq-content.ts
git commit -m "feat: implement seo-friendly faq section"
```

## Chunk 2: Verification

### Task 2: Verify Standards

- [ ] **Step 1: Run build and lint**

Run: `npm run lint && npm run typecheck && npm run build`
Expected: PASS

- [ ] **Step 2: Final commit**

```bash
git commit -am "chore: verify faq implementation"
```
